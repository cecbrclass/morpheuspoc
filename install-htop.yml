---

- name: Install
  gather_facts: false
  hosts: all
  become: true
  connection: local
  tasks: 


    - name: Apt Install htop
      ansible.builtin.apt:
        name: htop
        state: latest
        update_cache: yes
